"""Schema for processor implementations."""

from app.source.repositories.base.schema import BaseSettings

class BaseProcessorJobSettings(BaseSettings):
    """Base schema for processor job settings."""
