"""Schema for sender implementations."""

from app.project_app.repositories.base.schema import BaseSettings


class BaseSenderJobSettings(BaseSettings):
    """Base schema for sender job settings."""
